// This is a backend wire-protocol name. Keep the legacy BB prefix until the
// BuilderLab backend migrates the header contract.
pub const SESSION_CREDENTIAL_HEADER: &str = "X-BB-Session-Credential";
